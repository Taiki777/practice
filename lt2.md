# Linuxを学んで、調査の入口が少し見えるようになった

## 背景
最近、Linuxについて学習中  
目的は実務で不具合や障害に遭遇したときに、何を見ればよいのかの解像度を上げること

これまでは「CPU使用率が高い」「メモリ使用量が多い」「ディスクがいっぱい」といった言葉を聞いても、現象としては分かっても、それが実際に何を意味していて、どこから調査すべきかが曖昧だった。  
今回の学習を通して、原因をすぐ特定できるようになったわけではないが、少なくとも調査の入口としてどの観点を持てばよいかが少し見えるようになった。

この記事では、学習前に曖昧だった点と、Linuxの仕組みを学ぶことで実務の見え方がどう変わったかを整理する。


## 学習前の状態
学習前は、障害や不具合に関連する以下のような言葉の意味を、断片的にしか理解できていなかった。

- CPU使用率が高い
- メモリ使用量が多い
- ディスク容量が逼迫している
- プロセスが増えている
- ログが出続けている

たとえば「CPU使用率が高い」と聞いても、単に重い状態らしいという認識で、  
どの処理がCPUを使っているのか、あるいはCPUではなく待ち時間が長いのかまでは考えられていなかった。

また、「ログが増え続けている」と聞いても、ログファイルが大きくなること自体の問題はなんとなく分かっていても、  
それがどのプロセスによる書き込みなのか、ディスク逼迫や他機能への影響とどうつながるのかまではイメージできていなかった。


-> 現象の名前は知っていても、OS・プロセス・メモリ・ファイルといった要素がどう関係しているかが整理できていなかった。



## 学習して分かったこと

## 1. プロセスは「動いている実体」だと理解できた
学習前は、プログラムとプロセスの違いをあまり意識できていなかった。  
しかし学習を通して、プログラムはディスク上に置かれたコードであり、プロセスはそれが実行されている状態の実体であると理解できた。

この理解によって、何か問題が起きたときに「いま実際に何が動いているのか」を見る意識が持てるようになった。  
たとえば以下のような見方である。

- CPUを多く使っているのはどのプロセスか
- メモリを多く消費しているのはどのプロセスか
- どのプロセスがログファイルを書き続けているのか

実務ではアプリケーションコードだけを見ても分からないことがあり、  
まずは「その現象を起こしている実体は何か」をプロセスの観点で捉えることが重要だと感じた。



## 2. 実行時間は「遅い = CPUが重い」ではないと分かった
`time` コマンドの `real` `user` `sys` を学んだことで、処理時間の見方も変わった。

- `real`: 実際に経過した時間
- `user`: ユーザ空間でCPUを使った時間
- `sys`: カーネル空間でCPUを使った時間

これまでは、処理に時間がかかっていると「CPU負荷が高いのだろう」と雑に捉えがちだった。  
しかし実際には、待ち時間が長いだけの可能性もあるし、システムコールによるカーネル側の処理時間が多い可能性もある。

この理解によって、処理が遅いときに

- CPU計算が多いのか
- I/O待ちが多いのか
- OS側の処理が多いのか

といった見方が少しできるようになった。  
まだ詳細に分析できるレベルではないが、「時間がかかる」という結果を一段分解して考えられるようになったのは大きいと感じている。


### 3. ログは「ただ見るもの」ではなく、プロセスが書いているファイルだと分かった
Linuxでは、多くのものをファイルとして扱うという考え方がある。  
この考え方を知ったことで、ログファイルも単なる出力結果ではなく、プロセスが実際に書き込んでいる対象として捉えられるようになった。

学習前は、ログはアプリケーションの実行結果を確認するために読むもの、という理解が中心だった。  
しかし学習を通して、ログはプロセスによって継続的に書き込まれるファイルであり、どのプロセスがどのファイルを開いているかという観点が重要だと分かった。

この理解によって、たとえば以下のような見方ができるようになった。

- どのプロセスがログを出力しているのか
- どのファイルに出力しているのか
- ログ出力が問題の現象とどう関係しているのか

実務ではログを読むことは多いが、ログを「誰が・どこに・どう書いているか」まで意識することで、現象の捉え方が少し変わったと感じている。


## 実務での見え方がどう変わったか
今回の学習を通して最も大きかったのは、障害や不具合に対して「どこから見始めればよいか」の入口が少し分かるようになったことである。

以前は、問題が起きたときにアプリケーションのコードやエラーメッセージだけを見ようとしがちだった。  
しかし今は、少なくとも次のような観点を持てるようになった。

- どのプロセスが動いているか
- CPUを使っているのは何か
- 実行時間の内訳はどうか
- ログはどこに出ていて、増え続けていないか
- ディスク容量に問題はないか
- OSの仕組みとして何が起きうるか

たとえば「処理が遅い」という現象が起きたときも、以前はアプリケーションコードやエラーメッセージだけを見ようとしていた。  
しかし今は、まずCPUを使っているプロセスは何か、処理時間の内訳はどうか、I/O待ちが多い可能性はないか、といった観点から切り分けを始める意識を持てるようになった。  
  
また、ログに関しても、単に内容を読むだけでなく、どのプロセスがどのファイルに出力しているのか、そのログ出力が現象とどう関係しているのか、という見方ができるようになった。

もちろん、これだけで原因特定ができるわけではない。  
ただ、何も分からない状態で調査に入るのと比べると、見るべき対象を切り分けるための土台ができたと感じている。



## 感想
今回の学習で感じたのは、Linuxの知識は直接機能開発に使うものではなくても、実務で問題が起きたときの「解像度」を上げるための基礎になるということだった。

障害対応や調査においていきなり原因を特定するのは難しい。  
そのため、まずは

- 何が起きていそうか
- どこを見ればよいか
- どの観点で切り分けるか

を考えられるようになることが重要だと思う。  
Linuxの仕組みを学ぶことは、その最初の一歩としてかなり意味があると感じた。

今後も、Linuxの仕組みの理解を深めながら、実務で起きる現象をより構造的に捉えられるようにしていきたい。